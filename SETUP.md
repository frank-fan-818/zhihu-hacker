# 你需要完成的配置

代码已经支持配置加载、知乎登录、主题问题推荐与回答摘要。真实服务尚未联调：你目前未申请凭据和域名。本机仍可运行规则初筛。

## 第一步：申请知乎数据接口凭据

打开 https://developer.zhihu.com/profile ，登录并按页面指引创建 Access Secret。如果账号无入口或接口权限不足，向活动技术支持确认。把真实值填入本目录 `.env` 的 `ZHIHU_ACCESS_SECRET=` 后面。不要使用教程截图里的值，不需要发送到聊天。

作用：知乎搜索、全网搜索、主题问题推荐、回答摘要和额度查询。它与 OAuth App Key 不同。

## 第二步：配置审稿模型

在 `.env` 填写你已有模型服务的三项配置：

```text
MODEL_BASE_URL=
MODEL_NAME=
MODEL_API_KEY=
```

基础地址为提供商的 Chat Completions 基础地址，不含最后的 `/chat/completions`；例如接口完整路径以 `/v1/chat/completions` 结束，填写到 `/v1`。需要支持 JSON 输出。本轮没有选择付费提供商或购买额度；如果没有模型服务，先告诉我你打算使用哪一家，我再核对兼容性。

缺少模型时仅使用本地规则；知乎 Secret 不能直接填到 MODEL_API_KEY 代替结构化审稿模型。知乎直答尚未接入。

## 第三步：申请知乎登录应用凭据

打开赛事项目页面： https://www.zhihu.com/hackathon?activity_code=zhihu_hackathon_2026_p2 。在你的项目中寻找 App ID/App Key 入口。PDF 提供了该入口的示意，实际是否已开放以页面为准；未显示时联系赛事支持。

填写 `.env` 中 `ZHIHU_OAUTH_APP_ID`、`ZHIHU_OAUTH_APP_KEY`。App ID 和领取状态可以告诉我，App Key 留在本机配置中。

先不用购买域名或猜回调地址。确定部署方案与实际 HTTPS 地址后，再同时填写：

```text
APP_ORIGIN=https://实际应用域名
ZHIHU_OAUTH_REDIRECT_URI=https://实际应用域名/auth/zhihu/callback
```

在知乎项目页面登记完全相同的回调地址。当前实现要求这个固定路径，不带尾斜杠、Query 或 fragment；APP_ORIGIN 只包含协议和域名。两项没有完成前，页面提示登录未配置。

如果部署到 Vercel 等无服务器平台，还需要第四项：共享键值存储（Upstash Redis REST），
否则发起登录与接收回调会落在不同实例，登录必定失败。

```text
SESSION_STORE_URL=https://xxx.upstash.io
SESSION_STORE_TOKEN=xxxxxxxx
```

完整步骤、环境变量清单与限制见 [DEPLOY.md](DEPLOY.md)。

本应用有 SQLite 写入，需要持久磁盘；不要直接使用“无数据库写入”的部署方案。公网部署、多实例和备份仍需单独完成。

## 第四步：启动与验证

在应用目录运行：

```powershell
npm.cmd run diagnose
npm.cmd start
```

`diagnose` 只检查配置，不联网、不显示密钥；`verified:false` 表示没有验证真实连接。修改 `.env` 后需停止旧服务再重启。

需要确认真实知乎鉴权与额度时，主动运行 `npm.cmd run quota`。它向知乎查询当前凭据的额度，不向网页公开用量。诊断结果只需要告诉我成功/失败和错误码，不要发送密钥。

回调配置完成后，你需要亲自在知乎授权页完成最终确认。真实验收顺序：

1. 非敏感示例执行查看依据，两类搜索返回真实来源。
2. 输入主题找问题，选题后查看真实回答摘要。
3. 模型初筛及依据修改输出有效，并逐句检查是否保留意图。
4. 登录时选择只关联当前草稿；确认登录后该稿存在，退出后其他匿名草稿仍存在；再次登录恢复账号稿。

## 当前实现边界

- `.env` 已创建为空模板，已在 .gitignore 中排除；npm start 会加载它，系统环境变量优先。
- OAuth 单实例内存会话，服务重启后重新登录；账号项目仍在 SQLite。身份使用稳定知乎字符串标识的哈希，不需要保存邮箱或电话。
- 只用 OAuth Token 获取一次基础身份，之后丢弃 Token；尚未读取用户收藏/创作列表。后续接这些能力需另行设计 Token 存储。
- 登录只关联用户选择的一篇匿名草稿，不自动迁移所有项目。账号达到 30 篇或当前项目存在运行任务时，迁移拒绝，原稿保留。
- 回答分页当前替换展示上一页，不累积整条讨论；摘要不会自动变成证据。
- 操作、选题与回答接口有本机预算控制，但不替代公网网关限流。仍需正式上线检查。
- 搜索高级筛选、逐句引用导出、知识库、直答与创作复盘未在这轮实现。

## 本轮验证

30 项自动测试通过；语法检查通过。真实浏览器验证缺少配置提示、隔离夹具下的问题起稿/回答摘要，375px 内容区无水平溢出。测试夹具服务已关闭，测试资料未写入正式项目数据库。真实知乎/模型成功链路等待凭据，尚未验收。
