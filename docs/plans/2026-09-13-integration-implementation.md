# 接入实施计划

用户已授权直接推进。沿用校园季接入审查方案，实施 A/B/C，不安装官方 CLI，不上传文件、不自动授权或部署。

1. 配置：.env.example 与 Node 环境加载，命令行诊断/手动额度查询，Secret 不进入页面。
2. 搜索：业务错误分型，保留来源元数据，参数保持兼容；不自动发起业务探活。
3. OAuth：独立模块，state 单次/过期/会话绑定，服务端 Token 与身份会话，显式当前草稿迁移，退出轮换匿名 Cookie。单实例登录会话内存保存，重启需重新登录；稳定账号 ID 对应 SQLite 项目。
4. 问题入口：按主题推荐，用户选问题建立项目上下文，主动查看回答摘要、服务端分页，不作为事实证据自动引用。
5. 验收：已有测试回归，配置、鉴权、OAuth 重放/隔离/过期、迁移、问题分页契约测试；尝试真实浏览器验收。无真实凭据部分不宣称线上成功。

等待用户提供的非秘密信息：OAuth 是否已领取、App ID、正式域名；真实密钥在本机 .env 填写。不得用示例凭据或临时猜测回调替代。

## 实施结果

A/B/C 基础代码已完成，30 项测试通过、脚本语法检查通过，真实浏览器验证未配置提示和隔离夹具下的问题起稿/摘要展示。登录使用稳定账号标识映射 SQLite 项目，内存会话重启需重新登录；匿名 Cookie 单独保存。用户表示尚未申请凭据和域名，具体办理步骤见 cognitive-debugger/SETUP.md。真实业务请求、HTTPS OAuth 联调、正式部署尚未执行。高级筛选、逐句引用与知识库仍为后续范围。
